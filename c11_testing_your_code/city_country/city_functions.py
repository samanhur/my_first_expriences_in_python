def city_country_name(city, country, population=''):
    """
    Getting the city and the country name and return in a formatted way
    """
    formatted_name = f"{city}, {country}"
    
    return formatted_name.title()